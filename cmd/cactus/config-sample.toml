[core]
addr = ":5050"

[data]
# db = "cactus.db"          # Path to the SQLite database file
# blobs = "cactus.bs"       # Path to the blob store directory

[belt]
size = 2

[sandbox]
# mode = "jail"             # Options: "jail" (ptrace, default), "docker"
# docker_image = "cactus-sandbox"
